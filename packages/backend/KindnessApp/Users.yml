info:
  name: Users
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/users
  body:
    type: json
    data: |-
      {
        "name": "Test1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
